version: "3.9"

services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: neuralgate
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d neuralgate"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 15s

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # proxy:                          # Uncomment Day 3
  #   build: ./proxy
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     DATABASE_URL: postgresql://postgres:postgres@postgres:5432/neuralgate
  #     REDIS_URL: redis://redis:6379
  #     ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
  #     OPENAI_API_KEY: ${OPENAI_API_KEY}
  #     GOOGLE_API_KEY: ${GOOGLE_API_KEY}
  #     XAI_API_KEY: ${XAI_API_KEY}
  #     DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
  #     MISTRAL_API_KEY: ${MISTRAL_API_KEY}
  #     CACHE_SIMILARITY_THRESHOLD: "0.95"
  #     DEFAULT_CACHE_TTL_HOURS: "24"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   restart: unless-stopped

  # dashboard:                      # Uncomment Day 6
  #   build: ./dashboard
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     VITE_API_URL: http://localhost:8000
  #   depends_on:
  #     - proxy

  # prometheus:                     # Uncomment Day 5
  #   image: prom/prometheus:latest
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
  #     - prometheusdata:/prometheus
  #   depends_on:
  #     - proxy

volumes:
  pgdata:
  redisdata:
  # prometheusdata:                 # Uncomment Day 5
